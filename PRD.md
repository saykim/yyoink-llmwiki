# yyoink - Product Requirements Document (PRD)

## 1. 제품 개요

### 1.1 제품명

**yyoink**

### 1.2 버전

v1.0.0

### 1.3 제품 설명

웹 서핑 중 발견한 텍스트, 정보, 인사이트를 수집하고 프로젝트별로 체계적으로 정리할 수 있는 Chrome 확장 프로그램. 연구, 리서치, 콘텐츠 수집에 최적화된 도구.

### 1.4 핵심 가치 제안

- **간편한 수집**: 우클릭 한 번으로 선택한 텍스트 저장
- **프로젝트별 정리**: 색상 코드가 있는 프로젝트로 스니펫 분류
- **복사 방지 해제**: 복사가 막힌 웹사이트에서도 텍스트 추출 가능
- **오프라인 저장**: 모든 데이터는 로컬에 저장되어 프라이버시 보장

---

## 2. 타겟 사용자

### 2.1 주요 사용자 (Primary Users)

| 페르소나          | 설명                                 | 니즈                                       |
| ----------------- | ------------------------------------ | ------------------------------------------ |
| 연구자/리서처     | 논문, 기사 등에서 정보 수집          | 출처 URL과 함께 정보 저장, 프로젝트별 분류 |
| 콘텐츠 크리에이터 | 블로그, 영상 기획을 위한 자료 수집   | 빠른 클리핑, 메모 추가 기능                |
| 학생              | 과제, 논문 작성을 위한 참고자료 수집 | 검색, 내보내기 기능                        |
| 지식 근로자       | 업무 관련 정보 스크랩                | 빠른 접근, 전체 복사 기능                  |

### 2.2 사용 시나리오

1. 웹 서핑 중 유용한 문장 발견 → 우클릭으로 즉시 저장
2. 특정 주제 리서치 → 프로젝트 생성 후 관련 정보만 필터링
3. 복사 방지 사이트에서 텍스트 필요 → 복사 해제 모드 활성화
4. 수집한 자료 정리 → 마크다운/텍스트로 내보내기

---

## 3. 기능 요구사항

### 3.1 핵심 기능 (Core Features)

#### 3.1.1 스니펫 저장

| 기능                 | 설명                                          | 우선순위 |
| -------------------- | --------------------------------------------- | -------- |
| 우클릭 저장          | 선택한 텍스트를 우클릭 메뉴에서 저장          | P0       |
| 프로젝트 선택 저장   | 저장 시 특정 프로젝트 직접 선택 가능          | P0       |
| 활성 프로젝트 저장   | 현재 선택된 프로젝트로 빠르게 저장            | P1       |
| 메타데이터 자동 수집 | URL, 페이지 제목, 도메인, 저장 시각 자동 기록 | P0       |

#### 3.1.2 프로젝트 관리

| 기능            | 설명                                       | 우선순위 |
| --------------- | ------------------------------------------ | -------- |
| 프로젝트 생성   | 이름과 색상으로 새 프로젝트 생성           | P0       |
| 프로젝트 삭제   | 프로젝트 삭제 (스니펫 삭제 또는 이동 옵션) | P0       |
| 프로젝트 필터링 | 특정 프로젝트의 스니펫만 표시              | P0       |
| 색상 코드       | 8가지 색상 중 선택하여 프로젝트 구분       | P1       |
| 스니펫 카운트   | 각 프로젝트의 스니펫 수 표시               | P2       |

#### 3.1.3 스니펫 관리

| 기능        | 설명                                         | 우선순위 |
| ----------- | -------------------------------------------- | -------- |
| 스니펫 목록 | 저장된 스니펫을 카드 형태로 표시             | P0       |
| 스니펫 검색 | 텍스트 내용으로 검색 (실시간, debounce 적용) | P0       |
| 스니펫 편집 | 저장된 텍스트 내용 수정, 프로젝트 변경       | P0       |
| 스니펫 복사 | 개별 스니펫 클립보드 복사                    | P0       |
| 스니펫 삭제 | 인라인 확인 UI로 실수 방지 후 삭제           | P0       |
| 전체 복사   | 현재 보이는 모든 스니펫 일괄 복사            | P1       |
| 확장/축소   | 긴 스니펫 내용 펼쳐보기/접기                 | P1       |

#### 3.1.4 복사 방지 해제 (Copy Bypass)

| 기능             | 설명                                             | 우선순위 |
| ---------------- | ------------------------------------------------ | -------- |
| 일반 모드        | CSS user-select 속성 해제, JS 이벤트 핸들러 제거 | P0       |
| 강력 모드        | JavaScript 완전 비활성화 (길게 누르기로 활성화)  | P1       |
| 오버레이 제거    | 복사 방지용 투명 오버레이 요소 자동 제거         | P1       |
| 흐린 이미지 복원 | 네이버 블로그 등의 블러 처리된 이미지 복원       | P2       |

#### 3.1.5 추가 수집 도구

| 기능              | 설명                                    | 우선순위 |
| ----------------- | --------------------------------------- | -------- |
| 페이지 캡처       | 현재 페이지 본문 전체를 스니펫으로 저장 | P1       |
| 강제 선택         | 선택 불가능한 텍스트 강제 추출          | P1       |
| 퀵 메모           | 직접 입력한 메모를 스니펫으로 저장      | P1       |
| 클립보드 붙여넣기 | 클립보드 내용을 즉시 스니펫으로 저장    | P2       |

### 3.2 데이터 관리 기능

#### 3.2.1 내보내기 (Export)

| 형식       | 설명                  | 포함 데이터                  |
| ---------- | --------------------- | ---------------------------- |
| JSON       | 완전한 데이터 백업    | 모든 스니펫 + 프로젝트 정보  |
| Markdown   | 가독성 높은 문서 형태 | 프로젝트별 그룹핑, 출처 링크 |
| Plain Text | 단순 텍스트           | 스니펫 텍스트만              |

#### 3.2.2 가져오기 (Import)

| 형식   | 설명                       |
| ------ | -------------------------- |
| JSON   | 이전에 내보낸 데이터 복원  |
| TXT/MD | 줄 단위로 개별 스니펫 생성 |

### 3.3 UI/UX 기능

#### 3.3.1 사이드 패널

| 기능          | 설명                                                  |
| ------------- | ----------------------------------------------------- |
| 사이드 패널   | Chrome 기본 사이드 패널에서 동작 (탭 전환 가능)       |
| 키보드 단축키 | `Cmd+Shift+P` (Mac) / `Ctrl+Shift+P` (Windows)로 토글 |
| 검색 단축키   | `Cmd+K` / `Ctrl+K`로 검색창 포커스                    |

#### 3.3.2 테마

| 테마  | 설명             |
| ----- | ---------------- |
| Auto  | 시스템 설정 따름 |
| Light | 밝은 테마        |
| Dark  | 어두운 테마      |

#### 3.3.3 알림

- **Toast 알림**: 저장, 복사, 삭제 등 액션 완료 시 하단에 표시

---

## 4. 기술 요구사항

### 4.1 플랫폼

- **브라우저**: Chrome (Manifest V3)
- **최소 버전**: Chrome 88 이상

### 4.2 권한 (Permissions)

| 권한              | 용도                            | 필수 여부 |
| ----------------- | ------------------------------- | --------- |
| `storage`         | 스니펫 및 프로젝트 데이터 저장  | 필수      |
| `contextMenus`    | 우클릭 메뉴에 저장 옵션 추가    | 필수      |
| `activeTab`       | 현재 탭 정보 접근 (URL, 제목)   | 필수      |
| `clipboardWrite`  | 스니펫 복사 기능                | 필수      |
| `clipboardRead`   | 클립보드에서 붙여넣기 기능      | 필수      |
| `sidePanel`       | 사이드 패널 UI                  | 필수      |
| `contentSettings` | 강력 모드 (JavaScript 비활성화) | 조건부    |
| `<all_urls>`      | 모든 웹사이트에서 동작          | 필수      |

### 4.3 데이터 저장

- **저장소**: `chrome.storage.local`
- **용량 제한**: 약 5MB (일반), 확장된 제한 시 더 큼
- **데이터 구조**:
  ```json
  {
    "snippets": [
      {
        "id": "string",
        "text": "string",
        "sourceUrl": "string",
        "pageTitle": "string",
        "domain": "string",
        "projectId": "string",
        "createdAt": "ISO8601"
      }
    ],
    "projects": [
      {
        "id": "string",
        "name": "string",
        "color": "#hexcolor",
        "createdAt": "ISO8601"
      }
    ]
  }
  ```

### 4.4 아키텍처

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│   Side Panel    │────▶│ Background.js    │────▶│  Content.js     │
│   (main.js)     │     │ (Service Worker) │     │  (웹 페이지)     │
└────────┬────────┘     └────────┬─────────┘     └─────────────────┘
         │                       │
         └───────────┬───────────┘
                     ▼
           ┌─────────────────┐
           │ Chrome Storage  │
           │   (Local)       │
           └─────────────────┘
```

---

## 5. 비기능 요구사항

### 5.1 성능

- 사이드 패널 로딩: 500ms 이내
- 스니펫 저장: 100ms 이내 완료
- 검색: 입력 후 300ms debounce 적용
- 스크롤: 1000개 스니펫까지 부드러운 스크롤 유지

### 5.2 보안 및 프라이버시

- 모든 데이터는 로컬에만 저장 (외부 서버 전송 없음)
- 민감한 정보 암호화 없음 (Chrome Storage 자체 보안 의존)

### 5.3 접근성

- 키보드 네비게이션 지원
- 화면 리더 기본 호환

---

## 6. 제한사항 및 향후 계획

### 6.1 현재 제한사항

- 이미지, 파일 저장 미지원 (텍스트만)
- 클라우드 동기화 미지원
- 다중 브라우저 미지원 (Chrome 전용)
- 협업 기능 없음

### 6.2 향후 개선 고려

| 기능            | 설명                | 우선순위 |
| --------------- | ------------------- | -------- |
| 태그 시스템     | 스니펫에 태그 추가  | P1       |
| AI 요약         | 긴 텍스트 자동 요약 | P2       |
| 클라우드 동기화 | Google Drive 연동   | P2       |
| Firefox 지원    | 크로스 브라우저     | P3       |

---

## 7. 파일 구조

```
context_pilot/
├── manifest.json           # 확장 프로그램 설정
├── background.js           # 서비스 워커 (컨텍스트 메뉴, 메시지 처리)
├── content.js              # 콘텐츠 스크립트 (복사 해제, 텍스트 추출)
├── sidepanel/
│   ├── index.html          # 사이드 패널 UI
│   ├── main.js             # 사이드 패널 로직
│   └── styles.css          # 스타일시트
└── icons/
    ├── icon16.png
    ├── icon32.png
    ├── icon48.png
    └── icon128.png
```

---

## 8. 릴리스 히스토리

| 버전  | 날짜    | 변경 사항   |
| ----- | ------- | ----------- |
| 1.0.0 | 2024-01 | 초기 릴리스 |

---

## 부록: 컨텍스트 메뉴 구조

```
Save to yyoink
├── [Project Name 1]
├── [Project Name 2]
├── ────────────────
├── Save to Active Project
└── + Create New Project...
```

---

_이 PRD는 다른 Chrome 확장 프로그램에 재사용할 수 있도록 작성되었습니다. 제품명, 목적, 핵심 기능을 수정하여 활용하세요._
